<%- include('partials/header') %>
<h1>Home</h1>
<p><%= homeStartingContent %></p>
<% posts.forEach(e => { %>
    <h1><%= e.title %></h1>
    <p><%= e.content.substring(0, 100) + " ..." %><a href="<%= 'posts/' + e._id %>">Read more</a></p>
<% }); %>
<%- include('partials/footer') %>